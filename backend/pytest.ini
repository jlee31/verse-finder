[pytest]
# `pytest` from backend/ needs app/ and evals/ importable.
pythonpath = .
testpaths = evals
markers =
    slow: loads the mpnet model / embedding matrix (~20s startup)
# Fast by default; opt into the slow ones with `-m slow` or `-m ""`.
addopts = -m "not slow"
