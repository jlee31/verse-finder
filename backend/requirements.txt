fastapi==0.104.1
uvicorn[standard]==0.24.0
# FastAPI 0.104.1 ships starlette 0.27, whose TestClient calls httpx with the
# `app=` shortcut that httpx 0.28 removed. Without this pin every API test in
# tests/test_api.py errors on construction. Nothing else here wants >=0.28:
# anthropic and langsmith both ask only for httpx<1.
httpx==0.27.2
# Raised from 2.5.0 by langchain. Verified against FastAPI 0.104.1: request
# parsing, response serialization, and OpenAPI generation all still pass.
pydantic==2.13.4

# RAG generation step
anthropic==0.120.2
python-dotenv==1.0.1


numpy==1.26.4
scikit-learn==1.4.2
torch==2.2.2
transformers==4.39.3
sentence-transformers==2.6.1

# The LangGraph agent (app/rag/agent_lc.py). Installing these raises pydantic
# and anthropic above; the pins at the top already reflect that.
langchain==1.3.14
langchain-anthropic==1.5.3
langgraph==1.2.10

# The explainer (lime) and the test runner live in requirements-dev.txt. They
# are not needed to serve a request, and lime alone drags in scikit-image and
# matplotlib — ~150 MB of image for an endpoint the deployed frontend never
# calls. `pip install -r requirements-dev.txt` gets everything, and that is
# what you want locally.
