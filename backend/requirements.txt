fastapi==0.104.1
uvicorn[standard]==0.24.0
# Raised from 2.5.0 by langchain. Verified against FastAPI 0.104.1: request
# parsing, response serialization, and OpenAPI generation all still pass.
pydantic==2.13.4

# RAG generation step
anthropic==0.120.2
python-dotenv==1.0.1


numpy==1.26.4
scikit-learn==1.4.2
torch==2.2.2
transformers==4.39.3
sentence-transformers==2.6.1

# Explainability: which query words drove the retrieval (the "R" in RAG)
lime==0.2.0.1

# The LangGraph agent (app/rag/agent_lc.py). Installing these raises pydantic
# and anthropic above; the pins at the top already reflect that.
langchain==1.3.14
langchain-anthropic==1.5.3
langgraph==1.2.10

# Eval harness (evals/) — run: pytest evals -q
pytest==9.1.1
