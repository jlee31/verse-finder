# Copy to .env and fill in your key:  cp backend/.env.example backend/.env
#
# Only needed for the generation step — the reflection the app writes about
# your quotes. Retrieval, /docs and the tests all work without it.
ANTHROPIC_API_KEY=sk-ant-...
